{
  "name": "spotifyradio",
  "version": "1.0.0",
  "description": "Código desenvolvido durante a Semana JavaScript Expert para aprender processamento de audio",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "start": "node server/index.js",
    "live-reload": "nodemon server/index.js",
    "test": "cross-env LOG_DISABLED=true NODE_OPTIONS=--experimental-vm-modules jest --no-cache",
    "test:watch": "cross-env LOG_DISABLED=true NODE_OPTIONS=--experimental-vm-modules jest --no-cache --watchAll",
    "test:coverage": "cross-env LOG_DISABLED=true NODE_OPTIONS=--experimental-vm-modules jest --no-cache --coverage tests/unit"
  },
  "engines": {
    "node": "17"
  },
  "keywords": [],
  "license": "ISC",
  "devDependencies": {
    "jest": "^27.5.1",
    "nodemon": "^2.0.15"
  },
  "dependencies": {
    "cross-env": "^7.0.3",
    "fs": "^0.0.1-security",
    "pino": "^7.8.0",
    "pino-pretty": "^7.5.3"
  }
}
